name: "Upload: Run Upload"

on:
  repository_dispatch:
    types: [custom_upload]

jobs:
  run_custom_action:
    runs-on: ubuntu-latest
    steps:
      - name: Print payload
        run: |
          echo "Manual database upload triggered!"
          echo "Payload: ${{ toJson(github.event.client_payload) }}"
